const Heading = ({ subtitle}: any) => {
    return (
        <section className="space-y-6">
            <p className="text-white text-base text-center font-semibold">
                {subtitle}
            </p>
        </section>
    )
}

export default Heading;